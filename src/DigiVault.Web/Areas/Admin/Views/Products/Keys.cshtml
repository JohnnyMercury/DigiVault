@model Product
@{
    ViewData["Title"] = $"Keys - {Model.Name}";
}

<div class="data-card text-center py-5">
    <i class="bi bi-key fs-1 mb-3" style="color: rgba(255,255,255,0.3);"></i>
    <h5>Ключи генерируются автоматически</h5>
    <p class="text-muted mb-3">Ключи для продуктов теперь создаются автоматически при покупке (GUID-формат)</p>
    <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-outline-light">
        <i class="bi bi-arrow-left me-1"></i>Назад к продукту
    </a>
</div>
