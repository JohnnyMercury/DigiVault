@using DigiVault.Core.Entities
@{
    ViewData["Title"] = "Подарочные карты";
    var giftCards = ViewBag.GiftCards as List<GiftCard> ?? new List<GiftCard>();
}

<style>
    .page-hero {
        padding: 3rem 0 2rem;
        text-align: center;
    }

    .page-hero-title {
        font-size: 2.5rem;
        font-weight: 800;
        margin-bottom: 0.5rem;
    }

    .page-hero-title span {
        background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .page-hero-subtitle {
        color: rgba(255, 255, 255, 0.5);
        font-size: 1.1rem;
    }

    .breadcrumb-nav {
        padding: 1rem 0;
    }

    .breadcrumb-nav a {
        color: rgba(255, 255, 255, 0.5);
        text-decoration: none;
        transition: color 0.3s ease;
    }

    .breadcrumb-nav a:hover {
        color: #f093fb;
    }

    .breadcrumb-nav span {
        color: rgba(255, 255, 255, 0.3);
        margin: 0 0.5rem;
    }

    .breadcrumb-nav .current {
        color: white;
    }

    /* Cards Grid */
    .cards-grid {
        display: grid;
        grid-template-columns: repeat(6, 1fr);
        gap: 1.25rem;
        padding: 1rem 0 2rem;
    }

    @@media (max-width: 1400px) {
        .cards-grid { grid-template-columns: repeat(5, 1fr); }
    }
    @@media (max-width: 1200px) {
        .cards-grid { grid-template-columns: repeat(4, 1fr); }
    }
    @@media (max-width: 992px) {
        .cards-grid { grid-template-columns: repeat(3, 1fr); }
    }
    @@media (max-width: 768px) {
        .cards-grid { grid-template-columns: repeat(2, 1fr); }
    }
    @@media (max-width: 480px) {
        .cards-grid { grid-template-columns: repeat(2, 1fr); gap: 1rem; }
    }

    .card-item {
        text-decoration: none;
        color: white;
        display: block;
        transition: all 0.3s ease;
    }

    .card-item:hover {
        transform: translateY(-5px);
        color: white;
    }

    .card-item-image {
        aspect-ratio: 1 / 1;
        border-radius: 16px;
        overflow: hidden;
        display: flex;
        align-items: center;
        justify-content: center;
        margin-bottom: 0.75rem;
    }

    .card-item-image img {
        width: 50%;
        height: 50%;
        object-fit: contain;
        object-position: center;
        transition: transform 0.3s ease;
        filter: drop-shadow(0 4px 12px rgba(0, 0, 0, 0.4));
    }

    .card-item:hover .card-item-image img {
        transform: scale(1.05);
    }

    .card-item-icon {
        font-size: 3.5rem;
        filter: drop-shadow(0 4px 8px rgba(0, 0, 0, 0.3));
    }

    .card-item-name {
        font-size: 0.9rem;
        font-weight: 600;
        text-align: left;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis;
    }

    /* Info Banner */
    .info-banner {
        background: linear-gradient(135deg, rgba(240, 147, 251, 0.1) 0%, rgba(245, 87, 108, 0.1) 100%);
        border: 1px solid rgba(240, 147, 251, 0.2);
        border-radius: 16px;
        padding: 1.5rem;
        margin-bottom: 2rem;
        display: flex;
        align-items: center;
        gap: 1rem;
    }

    .info-banner-icon {
        width: 50px;
        height: 50px;
        border-radius: 12px;
        background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        flex-shrink: 0;
    }

    .info-banner-content h4 {
        font-weight: 600;
        margin-bottom: 0.25rem;
    }

    .info-banner-content p {
        color: rgba(255, 255, 255, 0.6);
        margin: 0;
        font-size: 0.9rem;
    }

</style>

<!-- Breadcrumb -->
<section class="breadcrumb-nav">
    <div class="container">
        <a href="/">Главная</a>
        <span>/</span>
        <a href="/Catalog">Каталог</a>
        <span>/</span>
        <span class="current">Подарочные карты</span>
    </div>
</section>

<!-- Hero -->
<section class="page-hero">
    <div class="container">
        <h1 class="page-hero-title">
            <span>Подарочные карты</span>
        </h1>
        <p class="page-hero-subtitle">Подарки для себя и близких</p>
    </div>
</section>

<!-- Info Banner -->
<section class="pb-2">
    <div class="container">
        <div class="info-banner">
            <div class="info-banner-icon">
                <i class="bi bi-gift-fill"></i>
            </div>
            <div class="info-banner-content">
                <h4>Мгновенная доставка кодов</h4>
                <p>Получите код на email сразу после оплаты. Активируйте на любом устройстве.</p>
            </div>
        </div>
    </div>
</section>

@if (giftCards.Any())
{
    <section class="pb-5">
        <div class="container">
            <div class="cards-grid">
                @foreach (var card in giftCards)
                {
                    var hasImage = !string.IsNullOrEmpty(card.ImageUrl);
                    <a href="/Catalog/GiftCard/@card.Slug" class="card-item">
                        <div class="card-item-image" style="background: @card.Gradient;">
                            @if (hasImage)
                            {
                                <img src="@card.ImageUrl" alt="@card.Name" />
                            }
                            else
                            {
                                <span class="card-item-icon">@card.Icon</span>
                            }
                        </div>
                        <div class="card-item-name">@card.Name</div>
                    </a>
                }
            </div>
        </div>
    </section>
}
